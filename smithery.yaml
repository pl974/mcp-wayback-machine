build:
  dockerfile: Dockerfile
  dockerBuildPath: .

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    config => ({ command: 'node', args: ['dist/index.js'] })
