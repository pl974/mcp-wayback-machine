version: 1

build:
  dockerfile: Dockerfile

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    config => ({ command: 'node', args: ['dist/index.js'] })
